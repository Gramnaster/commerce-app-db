json.status do
  json.code 200
  json.message "Logged in successfully."
end

json.data do
  # Basic identity
  json.id @user.id
  json.email @user.email
  # json.first_name resource.first_name
  # JWT token identifier
  json.jti @user.jti
end

# if user.user_details
# json.user_details do
# json.id @user.user_details.id
# json.first @user.user_details.first_name
# json.last @user.user_details.last_name
# end
# end
